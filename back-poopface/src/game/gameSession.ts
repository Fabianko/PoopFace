// game/gameSession.ts
export class GameSession {
    gameId: string;
    // Aquí puedes añadir más propiedades como jugadores, estado del juego, etc.

    constructor(gameId: string) {
        this.gameId = gameId;
        // Inicialización de la sesión
    }

    // Métodos para manejar la lógica del juego
}
